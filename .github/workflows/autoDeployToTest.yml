# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: auto_non_stop_deploy

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  # test:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     - name: Use Node.js ${{ matrix.node-version }}
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: ${{ matrix.node-version }}
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run build
  #     - run: npm test

  deploy:
    # needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Create private_key and chmod
      - uses: actions/checkout@v3
      - run: touch private_key && chmod 600 private_key
      - run: echo "${{secrets.PRIVATE_EC2_PEM}}" > private_key
      - name: connection EC2 and deploy
      - run: ssh -o StrictHostKeyChecking=no -i private_key ubuntu@13.124.107.224 '
          cd ahtata-back &&
          git pull origin main &&
          sh deploy.sh
          '
